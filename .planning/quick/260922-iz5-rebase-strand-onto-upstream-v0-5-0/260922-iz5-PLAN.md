---
phase: quick-260922-iz5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # All paths below are inside the rebase worktree
  # C:/Users/admin/PycharmProjects/TradingEngineX-rebase-v050 (branch rebase/upstream-v0.5.0).
  # The main working tree is never written to (only `git branch` + `git worktree add` run there).
  - CHANGELOG.md
  - README.md
  - cli/main.py
  - tradingagents/default_config.py
  - tradingagents/graph/trading_graph.py
  - tests/test_budget.py
  - tests/test_memory_log.py
  - tests/test_structured_agents.py
  - .planning/STATE.md
  - scripts/PAPER_RUN.md          # only if the reflection-horizon limitation is verified as fixed upstream
  - .planning/MILESTONE.md        # only if a stated fact became false
autonomous: true
requirements: [QT-260922-iz5]

must_haves:
  truths:
    - "Branch rebase/upstream-v0.5.0 exists in its own worktree and has upstream v0.5.0 (2d17df8) as an ancestor"
    - "main, fork/main and all tags are unchanged; backup/pre-rebase-v0.5.0 points at 16bbe96; nothing was pushed"
    - "Budget enforcement (#582) works on both entry points (propagate and CLI) on top of upstream's #1249 checkpoint lifecycle: per-run reset, abort before the next LLM call, best-effort partial save, checkpoint kept on abort"
    - "Structured retry (#583), fail-fast credentials, the daily paper run, and CLI ticker validation (ours or upstream's equivalent) are all present at HEAD"
    - "Full pytest -q is green and ruff check . is clean on the rebased HEAD, run from the worktree against the worktree's code"
    - "scripts/daily_paper_run.py --preflight without credentials exits 20 with 'credentials missing' and makes no LLM or network call (the workflow maps 20 to a green run with a warning)"
    - ".planning/STATE.md on the branch has a 2026-09-22 Rebase-Protokoll entry, and Known Issues were re-checked against v0.5.0 code with file:line evidence"
  artifacts:
    - path: "C:/Users/admin/PycharmProjects/TradingEngineX-rebase-v050/tradingagents/graph/trading_graph.py"
      provides: "Upstream #1249 lifecycle + budget spend_tracker, reset in create_run_state, streamed _run_graph with budget partial save"
      contains: "spend_tracker.reset()"
    - path: "C:/Users/admin/PycharmProjects/TradingEngineX-rebase-v050/cli/main.py"
      provides: "Upstream CLI stream path (begin_checkpoint/checkpoint_input/end_checkpoint) + BudgetExceededError abort handling + BudgetConfigError/MissingAPIKeyError fail-fast"
      contains: "except BudgetExceededError"
    - path: "C:/Users/admin/PycharmProjects/TradingEngineX-rebase-v050/tests/test_budget.py"
      provides: "Budget tests moved from stream_run/clear_run_checkpoint onto the new seat"
    - path: "C:/Users/admin/PycharmProjects/TradingEngineX-rebase-v050/.planning/STATE.md"
      provides: "Rebase-Protokoll (2026-09-22)"
      contains: "Rebase-Protokoll (2026-09-22)"
  key_links:
    - from: "TradingAgentsGraph.create_run_state"
      to: "SpendTracker.reset"
      via: "reset as the first statement, before _resolve_pending_entries (reflector LLM calls count toward the run)"
      pattern: "spend_tracker\\.reset\\(\\)"
    - from: "TradingAgentsGraph._run_graph and cli/main.py run_analysis"
      to: "partial-save helper on BudgetExceededError"
      via: "except BudgetExceededError -> best-effort _log_state of last streamed state -> re-raise / Exit(1); record_decision and clear_checkpoint_on_success are skipped"
      pattern: "except BudgetExceededError"
    - from: "scripts/daily_paper_run.py"
      to: "TradingAgentsGraph.propagate(ticker, trade_date)"
      via: "positional call; upstream added optional portfolio=None and a possible 'REVIEW' signal"
      pattern: "graph\\.propagate\\(ticker, trade_date\\)"
---

<objective>
Rebase the TradingEngineX strand (17 commits a33fd4c..16bbe96) onto upstream TauricResearch/TradingAgents v0.5.0 (origin/main == 2d17df8) in an isolated worktree. Features we ported earlier are rebuilt on top of upstream's new code paths. Features upstream now owns are dropped, never reverted. The rebased HEAD must pass the pytest, ruff and paper-run gates, and it gets one final docs commit with the rebase protocol.

Purpose: The strand is 90 upstream commits behind (v0.4.0 + v0.5.0). The daily paper run (the strand's heartbeat) should run on the current framework, including the upstream fixes for CLI checkpoint resume (#1249), CLI decision log (4a9f196) and the holding window (#1169).
Output: local branch `rebase/upstream-v0.5.0` in worktree `C:/Users/admin/PycharmProjects/TradingEngineX-rebase-v050`, local backup branch `backup/pre-rebase-v0.5.0`, and a SUMMARY in the main repo's quick-task directory. Nothing is pushed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/MILESTONE.md
@./CLAUDE.md
@.planning/quick/260814-k1j-setz-die-p1-punkte-um-fang-mit-dem-v0-2-/260814-k1j-SUMMARY.md

NOTE: the main-tree .planning/STATE.md is DIRTY (operator edits and a bogus gsd-sdk frontmatter). Read the COMMITTED version for content (`git -C /c/Users/admin/PycharmProjects/TradingEngineX show 16bbe96:.planning/STATE.md`). Edit STATE.md only inside the worktree.

## Fixed constants (use these literals; shell state does not persist between Bash calls and cwd resets, so repeat them per command)

- MAIN=/c/Users/admin/PycharmProjects/TradingEngineX (main working tree; DIRTY with operator changes; read-only for this task except `git branch` / `git worktree add`)
- WT=/c/Users/admin/PycharmProjects/TradingEngineX-rebase-v050 (new worktree)
- PY=/c/Users/admin/PycharmProjects/TradingEngineX/.venv/Scripts/python.exe (Python 3.13.13, pytest 9.0.3)
- RUFF=/c/Users/admin/PycharmProjects/TradingEngineX/.venv/Scripts/ruff.exe (ruff 0.15.12, satisfies upstream's ruff>=0.15)
- BASE=2d17df8da1536c121e4d7395ac5a5dcec9e96d6f (origin/main == v0.5.0^{commit})
- OLD=16bbe96658159721f7d084c22cc993de275e664e (main == fork/main)
- The .venv has an EDITABLE install of tradingagents 0.2.4 that points at the MAIN tree. Every python/pytest call must run from $WT with PYTHONPATH=. so that the worktree's code is imported (T2 checks this explicitly). Do not pip install anything; upstream v0.5.0 only removed dependencies.
- Do not `git fetch`. The target is pinned to BASE so it cannot drift.

## Our 17 commits (oldest first), with expected handling

| Commit | Subject | Expected |
|---|---|---|
| e63c91e, c5407c7 | docs milestone / plan | .planning only; clean |
| a6f8581 | feat(budget) #582 | CONFLICT (default_config, trading_graph, cli/main, tests). Rebuild on the #1249 lifecycle (see design below) |
| 4a31720 | feat(structured) retry #583 | structured.py unchanged upstream (clean); test_structured_agents.py conflict: keep upstream's AND our tests |
| e02fc88 | feat(config) fail-fast creds | factory.py clean; cli/main.py conflict at graph construction; README secrets section |
| baf53d0 | fix(security) CLI ticker validation | cli/main.py; upstream now uses safe_ticker_component at cli/main.py:995 and :1339 (see criterion below) |
| 5452126, 5912fb5 | docs summary / STATE | .planning only |
| 3944ac3 | docs CHANGELOG Unreleased | CHANGELOG conflict; Unreleased goes above `## [0.5.0] — 2026-09-18` |
| 8eaadf5 | fix(budget) partial-save + checkpoint clear | Keep the best-effort partial save; drop clear_run_checkpoint (upstream clear_checkpoint_on_success + bbcd666 own it) |
| 402cd8b | ci: trigger fork CI | EMPTY; dropped via --no-keep-empty |
| 359bd03 | style ruff | may shrink or become empty; fine |
| ba576e3 | docs --max-cost correction | docs; the CLI has NO --max-cost flag, so none is added |
| 9916b84, 6257535, d6bd688, 16bbe96 | paper run / planning / ci fixes | our-only files; clean |

<interfaces>
Upstream v0.5.0 run lifecycle in tradingagents/graph/trading_graph.py (#1249). KEEP ALL OF THESE VERBATIM:
- propagate(self, company_name, trade_date, asset_type="stock", portfolio=None): validates the date, then `with self.checkpoint_scope(...) as tid: return self._run_graph(..., checkpoint_thread_id=tid, portfolio=portfolio)`
- begin_checkpoint(company_name, trade_date, asset_type="stock", portfolio=None) -> str | None  (recompiles with the saver, sets self._resuming)
- checkpoint_input(init_state): returns None when resuming, else init_state
- end_checkpoint(): restores the uncheckpointed graph
- checkpoint_scope(...): contextmanager wrapper of begin/end_checkpoint
- clear_checkpoint_on_success(company_name, trade_date, asset_type="stock", portfolio=None)
- create_run_state(company_name, trade_date, asset_type="stock", portfolio=None): `_resolve_pending_entries` (reflector LLM calls happen here) + past_context + instrument_context + portfolio_context. Documented as "propagate() and the CLI both start here"
- settle_pending(company_name), record_decision(company_name, trade_date, final_state)
- _run_signature(asset_type, portfolio=None); _fetch_returns(..., holding_days=5) requires `len(stock) > holding_days` (line ~323, #1169)
- _run_graph(company_name, trade_date, asset_type="stock", checkpoint_thread_id=None, portfolio=None): create_run_state -> get_graph_args -> inject thread_id -> checkpoint_input -> (debug: stream + merge | else: graph.invoke) -> curr_state -> _log_state -> record_decision -> clear_checkpoint_on_success -> (final_state, process_signal(...)). The signal may be "REVIEW" (#1170).
- _log_state(trade_date, final_state): strict key access, ensure_ascii=False, safe_ticker_component path. Keep it verbatim; our strand's version was also strict.
- propagator.get_graph_args(callbacks=None) sets stream_mode="values", so each streamed chunk is a full state and merging chunks equals invoke's result.

Upstream v0.5.0 cli/main.py run_analysis(checkpoint=None, portfolio=None), around lines 1160-1300:
create_run_state -> get_graph_args(callbacks=[stats_handler]) -> begin_checkpoint -> thread_id injection -> `try: for chunk in graph.graph.stream(graph.checkpoint_input(init_agent_state), **args): ... trace.append(chunk)` -> merge the trace into final_state -> record_decision -> clear_checkpoint_on_success -> `finally: graph.end_checkpoint()`. ensure_api_key(provider) (34899bd) prompts for a missing key and persists it to .env at selection time, before `TradingAgentsGraph(` at about line 1065.

Our strand (16bbe96), to be removed or rebuilt:
- TradingAgentsGraph.stream_run(company_name, trade_date, *, asset_type, init_agent_state=None, graph_args=None): generator that set up the checkpoint, reset the budget, injected thread_id, and on BudgetExceededError did a best-effort _log_state(last chunk) and re-raised. REMOVE: upstream #1249 owns everything except the budget parts.
- TradingAgentsGraph.clear_run_checkpoint(...): REMOVE; it duplicates upstream clear_checkpoint_on_success.
- tradingagents/budget.py: SpendTracker(reset(), tokens_in, tokens_out, cost, raise_error=True), BudgetExceededError, BudgetConfigError, build_spend_tracker(config), ensure_rates_configured(rates, models). Unchanged, our-only file.
- default_config keys: max_cost_per_run=None, max_tokens_per_run=None, model_cost_rates={} (placed before checkpoint_enabled).
- scripts/daily_paper_run.py: run_ticker calls graph.propagate(ticker, trade_date) and graph.save_reports(...), catches BudgetExceededError, and reads graph.spend_tracker counters afterwards. Exit codes: 0 OK, 1 CONFIG, 3 RUN_FAILED, 4 BUDGET, 20 NO_CREDENTIALS (preflight only). The workflow (.github/workflows/daily-paper-run.yml, preflight step at about lines 110-136) maps 20 to a `::warning` and exit 0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backup ref, isolated worktree, rebase onto v0.5.0 with the budget rebuilt on upstream's #1249 lifecycle</name>
  <files>CHANGELOG.md, README.md, cli/main.py, tradingagents/default_config.py, tradingagents/graph/trading_graph.py, tests/test_budget.py, tests/test_memory_log.py, tests/test_structured_agents.py (all inside the worktree)</files>
  <action>
Step A, preconditions (per locked decisions 1 and 2). Assert with git -C on MAIN: `rev-parse origin/main` equals BASE, `rev-parse 'v0.5.0^{commit}'` equals BASE, `rev-parse fork/main` equals OLD, and `rev-parse main` equals OLD. If main has moved (the operator commits in parallel), do not stop. Branch from the OLD SHA anyway, and list the extra main commits in the SUMMARY as "not included, to be replayed on top later". Also assert that the worktree path does not exist and that neither `backup/pre-rebase-v0.5.0` nor `rebase/upstream-v0.5.0` exists. Record `git -C MAIN status --porcelain` in the SUMMARY as the before-snapshot.

Step B, refs and worktree. Run `git -C MAIN branch backup/pre-rebase-v0.5.0 OLD`, then `git -C MAIN worktree add -b rebase/upstream-v0.5.0 C:/Users/admin/PycharmProjects/TradingEngineX-rebase-v050 OLD`. These are the ONLY commands in this plan that write to the main repo. Never run checkout, reset, rebase, stash or clean in MAIN. From here on, run every command with `git -C WT` or `cd WT && ...`.

Step C, rebase (locked decision 3). In WT: `GIT_EDITOR=true git rebase --no-keep-empty --empty=drop BASE`. `--no-keep-empty` drops 402cd8b, which is empty from the start. `--empty=drop` drops commits that become empty because upstream already has them. At each conflict stop: edit files to resolve, `git add` them, then `GIT_EDITOR=true git rebase --continue`. If `--continue` reports that nothing is left to commit because the resolution equals upstream, run `GIT_EDITOR=true git rebase --skip` and record the commit as "dropped by resolution" with a reason. Do not use `git rebase -i` or `--abort`, except to restart from the backup when you are stuck (the branch can be reset inside WT with `git -C WT reset --hard OLD`; that is allowed because it only touches the rebase branch). Keep each replayed commit's message and author unchanged.

Step D, conflict policy (locked decision 4). Upstream wins on any behaviour it now owns. Our feature goes back on top of upstream's code path, never by reverting an upstream fix. Write a SUMMARY line for every non-trivial resolution: commit, file, what was kept and what was dropped.

Step E, a6f8581 feat(budget). This is the core rebuild; resolve it to the following design.
(1) trading_graph.py: keep upstream's whole #1249 lifecycle from the interfaces block, the portfolio parameters and the #1027 debug-print dedup, all unchanged. DELETE our `stream_run`: its checkpoint setup, thread-id injection, initial-state building and CLI-no-op fix are upstream's #1249, documented in upstream's CHANGELOG 0.4.0.
(2) Keep our budget wiring: the import of BudgetExceededError and build_spend_tracker, and in `__init__` `self.spend_tracker = build_spend_tracker(self.config)`, appended to `self.callbacks` before the LLM clients are built.
(3) Per-run reset: the first statement of `create_run_state` is `if self.spend_tracker is not None: self.spend_tracker.reset()`, placed BEFORE `_resolve_pending_entries`. create_run_state is upstream's single shared entry for propagate and the CLI, and putting reset first makes the reflector's LLM spend count toward the run's cap. In our old strand, the reset came after the reflection and wiped that spend. Record this deliberate behaviour change in the SUMMARY.
(4) `_run_graph`: replace upstream's debug-stream / else-invoke split with ONE loop over `self.graph.stream(graph_input, **args)`. Print upstream's #1027 dedup messages only when `self.debug` is set. Merge each chunk into final_state and keep a reference to the latest chunk. Wrap the loop in `try/except BudgetExceededError` that calls a new private helper `_save_partial_state(self, trade_date, last_state)` and then re-raises. Streaming is required because partial save needs the last state; with stream_mode="values", the merged chunks equal invoke's result. Everything after the loop stays upstream's code and order (curr_state, _log_state, record_decision, clear_checkpoint_on_success, process_signal), so an abort skips record_decision and the checkpoint clear. `checkpoint_scope`'s finally still restores the graph.
(5) `_save_partial_state`: if last_state is not None, set curr_state and call `_log_state`, then log the warning "Run aborted by budget limit ... resume with --checkpoint". For a6f8581 either a plain call or the best-effort try/except from 8eaadf5 is acceptable. If you land the best-effort form here to avoid a second conflict round, say so in the SUMMARY; 8eaadf5 then shrinks to its remaining delta.
(6) cli/main.py run_analysis: keep upstream's #1249 block verbatim, including the `graph.graph.stream(graph.checkpoint_input(...))` loop and `finally: graph.end_checkpoint()`. Drop our `while True / next(stream)` loop and our `graph.stream_run(...)` call. Add on top of it: (a) `except BudgetConfigError` around `TradingAgentsGraph(` construction that prints a red message and raises typer.Exit(1) (e02fc88 later adds MissingAPIKeyError to the same except); (b) `except BudgetExceededError as exc` inside upstream's existing try, placed before its finally, that remembers the exception and calls `graph._save_partial_state(analysis_date, trace[-1] if trace else None)`. Record_decision and clear_checkpoint_on_success must NOT run on abort. After the finally, print "[red]Budget limit reached:[/red] ..." with the resume-with --checkpoint hint and raise typer.Exit(1). Do NOT add a --max-cost CLI flag; ba576e3 documents that the CLI has none.
(7) default_config.py: keep all of upstream's new keys and add our 3 budget keys.
(8) tests/test_budget.py is our file and applies cleanly, but it tests stream_run and clear_run_checkpoint. Rewrite those tests in this same stop, before `--continue`, so that every behaviour stays covered on the new seat: partial save plus re-raise on abort (via `_run_graph` or `_save_partial_state`, using the existing unbound-method plus MagicMock(spec=TradingAgentsGraph) pattern); no partial save on success; reset per run (via `create_run_state`); no record_decision and no clear_checkpoint_on_success on abort. Delete the clear_run_checkpoint test, because upstream's own checkpoint tests cover clear_checkpoint_on_success.
(9) tests/test_memory_log.py: take upstream's version. Upstream's test at about line 930 stubs `mock_graph.graph.invoke.return_value`; because `_run_graph` now streams, change that stub to `mock_graph.graph.stream` returning an iterator over [fake_state]. This is the only allowed edit to an upstream test caused by our code-path change; record it.
Before continuing past a6f8581, run `cd WT && PYTHONPATH=. PY -m pytest -q -x tests/test_budget.py tests/test_memory_log.py tests/test_checkpoint_lifecycle.py` and require it to be green.

Step F, the remaining conflicted commits.
- 4a31720: keep upstream's AND our test cases in tests/test_structured_agents.py, deduplicating only identical tests. Our structured.py change applies as-is because upstream did not touch structured.py. Then run `PYTHONPATH=. PY -m pytest -q -x tests/test_structured_agents.py` green.
- e02fc88: extend the construction except in cli/main.py to `(BudgetConfigError, MissingAPIKeyError)`. Keep upstream's ensure_api_key prompt flow unchanged. Check that ensure_api_key also puts the key into os.environ, not only .env, so fail-fast cannot fire right after a successful prompt; if it does not, record that as a finding and do not change upstream. Check that factory.py's `_required_env_vars` still resolves against upstream's current provider registry (upstream refreshed the model lists in 10cc070 and 673abb3). Run `pytest -q -x tests/test_api_key_failfast.py` green.
- baf53d0: keep our early check (safe_ticker_component before any disk write, red message, Exit(1)) UNLESS upstream's v0.5.0 CLI already rejects a path-escaping ticker before creating anything on disk and shows a user-facing error. In that case drop ours as a duplicate and cite the upstream line in the SUMMARY.
- 3944ac3 CHANGELOG: our "## [Unreleased] — TradingEngineX strand (rebased onto v0.5.0)" section goes directly above `## [0.5.0] — 2026-09-18`. Change the intro sentence to "ported onto upstream v0.5.0". DROP the "Shared stream_run() path" bullet, because upstream 0.4.0 documents #1249. Keep the budget, structured-retry, fail-fast and paper-run bullets, and the ticker-validation Fixed bullet if ours was kept. Keep upstream's sections and links byte-identical.
- 8eaadf5: keep its intent. `_save_partial_state` becomes best-effort (try/except Exception around `_log_state` with logger.exception) and must never mask the BudgetExceededError. The CLI abort message does not promise a saved state. DROP its clear_run_checkpoint part: upstream's clear_checkpoint_on_success runs after persistence in both `_run_graph` and the CLI, and bbcd666 also removes the DB sidecars. Move its new tests (for example "abort survives partial-save failure" and "keeps checkpoint on abort") to `_run_graph` / `_save_partial_state`. Re-run the Step E pytest command green.
- 359bd03 style: resolve, then run `RUFF check` on the touched files.
- Any other conflict: apply the same policy and write a SUMMARY line.
Record every commit that git dropped automatically (a clean cherry-pick of upstream, or empty) with the reason.
  </action>
  <verify>
    <automated>cd /c/Users/admin/PycharmProjects/TradingEngineX-rebase-v050 && GD=$(git rev-parse --git-dir) && test ! -d "$GD/rebase-merge" && test ! -d "$GD/rebase-apply" && test "$(git rev-parse --abbrev-ref HEAD)" = "rebase/upstream-v0.5.0" && git merge-base --is-ancestor 2d17df8da1536c121e4d7395ac5a5dcec9e96d6f HEAD && test "$(git rev-parse backup/pre-rebase-v0.5.0)" = "16bbe96658159721f7d084c22cc993de275e664e" && test "$(git log --format=%s 2d17df8da1536c121e4d7395ac5a5dcec9e96d6f..HEAD | grep -c 'ci: trigger fork CI')" = "0" && test -z "$(git grep -n -E '^(<<<<<<<|>>>>>>>) ' HEAD)" && test -z "$(git grep -n -E 'def stream_run|clear_run_checkpoint|\.stream_run\(' HEAD -- tradingagents cli tests scripts README.md CHANGELOG.md)" && git grep -n 'spend_tracker.reset()' HEAD -- tradingagents/graph/trading_graph.py && git grep -n 'except BudgetExceededError' HEAD -- cli/main.py tradingagents/graph/trading_graph.py && git grep -n -E 'begin_checkpoint|checkpoint_input|end_checkpoint' HEAD -- cli/main.py && PYTHONPATH=. /c/Users/admin/PycharmProjects/TradingEngineX/.venv/Scripts/python.exe -m pytest -q -x tests/test_budget.py tests/test_memory_log.py tests/test_checkpoint_lifecycle.py tests/test_structured_agents.py tests/test_api_key_failfast.py tests/test_daily_paper_run.py && git log --oneline 2d17df8da1536c121e4d7395ac5a5dcec9e96d6f..HEAD</automated>
  </verify>
  <done>No rebase is in progress. rebase/upstream-v0.5.0 contains BASE, and backup/pre-rebase-v0.5.0 is at 16bbe96. 402cd8b and any upstream-duplicate commits are gone, each with a recorded reason. HEAD has no stream_run or clear_run_checkpoint and no conflict markers. The budget reset sits in create_run_state; BudgetExceededError is handled in `_run_graph` and in the CLI on top of upstream's begin_checkpoint/checkpoint_input/end_checkpoint path. The targeted test files pass. Every non-trivial resolution has a SUMMARY line.</done>
</task>

<task type="auto">
  <name>Task 2: Gates on the rebased HEAD (pytest, ruff, paper-run preflight, structural invariants), each re-run and quoted verbatim</name>
  <files>none (verification only; fixes, if any, go into the same files as Task 1 inside the worktree)</files>
  <action>
Run every gate yourself from WT and paste the real output tail into the SUMMARY (locked decision 6). Do not rely on earlier runs or on memory.

G0, import sanity (the editable install points at the MAIN tree): `cd WT && PYTHONPATH=. PY -c "import tradingagents, cli; print(tradingagents.__file__); print(cli.__file__)"`. Both paths must be under TradingEngineX-rebase-v050. If they are not, stop and fix the invocation; every later gate would otherwise be testing the wrong tree.
G1, `cd WT && PYTHONPATH=. PY -m pytest -q`: 0 failed and 0 errors. Record the passed/skipped/subtest counts.
G2, `cd WT && RUFF check .`: "All checks passed!". Do not add ruff ignores or noqa to upstream code to get there. Also avoid Python 3.11+-only syntax in our code, because upstream CI runs 3.10.
G3, paper run without credentials, with no network and no paper-log writes. Make a temp dir with `mktemp -d` (outside both repos) and confirm there is no .env in WT (`test ! -e .env`). Run: `cd WT && env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY -u GOOGLE_API_KEY -u XAI_API_KEY -u OPENROUTER_API_KEY -u DEEPSEEK_API_KEY -u DASHSCOPE_API_KEY -u ZHIPUAI_API_KEY -u AZURE_OPENAI_API_KEY TRADINGAGENTS_LLM_PROVIDER=openai PYTHONPATH=. PY scripts/daily_paper_run.py --tickers AAPL,MSFT --date 2026-09-21 --max-cost 1.50 --out TMP/out --state TMP/state --preflight; echo rc=$?`. Expect rc=20 and stderr "preflight: credentials missing for provider 'openai': OPENAI_API_KEY". Run the same command without --preflight: expect rc=1 and "error: missing credentials", returned before the graph is built. Quote the workflow lines in .github/workflows/daily-paper-run.yml that turn rc 20 into `::warning` plus exit 0; that mapping is the documented "green with warning" path. `tests/test_daily_paper_run.py` inside G1 covers the run and budget paths with a fake graph.
G4, structural invariants in WT against BASE: `git diff --quiet BASE HEAD -- pyproject.toml` (pyproject stays upstream 0.5.0, per locked decision 5); `git diff --diff-filter=D --name-only BASE HEAD` must be empty (no upstream file deleted); `git diff --name-only BASE HEAD` must contain only .planning/**, .github/workflows/daily-paper-run.yml, CHANGELOG.md, README.md, cli/main.py, scripts/daily_paper_run.py, scripts/PAPER_RUN.md, scripts/paper_run_rates.json, tradingagents/budget.py, tradingagents/default_config.py, tradingagents/graph/trading_graph.py, tradingagents/agents/utils/structured.py, tradingagents/llm_clients/factory.py, and tests/test_budget.py, test_api_key_failfast.py, test_daily_paper_run.py, test_structured_agents.py, test_memory_log.py (justify anything else in the SUMMARY); no commit in BASE..HEAD is empty (loop over `git rev-list BASE..HEAD` with `git diff-tree --quiet c^ c`); `git grep -n -E "^\s*(import|from) (tqdm|redis|parsel|backtrader|langchain_experimental|setuptools|pkg_resources)\b" HEAD -- tradingagents cli scripts tests` must be empty, because upstream removed those deps and upstream CI does a clean install.
G5, outward invariants in MAIN: `git -C MAIN rev-parse main fork/main` both equal OLD (if main moved, it was the operator: report it, do not act); `git -C MAIN tag --points-at BASE` and `git -C MAIN tag -l 'v*'` are unchanged, with no new tag; `git -C MAIN status --porcelain` still shows the operator's entries (.gitignore, .planning/STATE.md, .claude/, .idea/, .mcp.json, .serena/, CLAUDE.md, scripts/graphify.sh, uv.lock), untouched by us. Nothing was pushed; no push command was run.
If a gate fails, fix it in WT. Prefer `git commit --fixup=<target sha>` followed by `GIT_EDITOR=true git rebase --autosquash --no-keep-empty BASE`, which is non-interactive (supported without -i since git 2.44; this machine has 2.55). The fallback is a standalone `fix(rebase): ...` commit ending with the trailer `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`. Re-run ALL gates after any fix, and record each fix in the SUMMARY.
  </action>
  <verify>
    <automated>cd /c/Users/admin/PycharmProjects/TradingEngineX-rebase-v050 && PY=/c/Users/admin/PycharmProjects/TradingEngineX/.venv/Scripts/python.exe && PYTHONPATH=. $PY -c "import tradingagents, cli; import sys; p=tradingagents.__file__+cli.__file__; sys.exit(0 if p.count('TradingEngineX-rebase-v050')==2 else 1)" && PYTHONPATH=. $PY -m pytest -q && /c/Users/admin/PycharmProjects/TradingEngineX/.venv/Scripts/ruff.exe check . && test ! -e .env && T=$(mktemp -d) && (env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY -u GOOGLE_API_KEY -u XAI_API_KEY -u OPENROUTER_API_KEY -u DEEPSEEK_API_KEY -u DASHSCOPE_API_KEY -u ZHIPUAI_API_KEY -u AZURE_OPENAI_API_KEY TRADINGAGENTS_LLM_PROVIDER=openai PYTHONPATH=. $PY scripts/daily_paper_run.py --tickers AAPL,MSFT --date 2026-09-21 --max-cost 1.50 --out "$T/out" --state "$T/state" --preflight; echo "rc=$?") 2>&1 | tee /dev/stderr | grep -q '^rc=20$' && git diff --quiet 2d17df8da1536c121e4d7395ac5a5dcec9e96d6f HEAD -- pyproject.toml && test -z "$(git diff --diff-filter=D --name-only 2d17df8da1536c121e4d7395ac5a5dcec9e96d6f HEAD)" && test "$(git -C /c/Users/admin/PycharmProjects/TradingEngineX rev-parse fork/main)" = "16bbe96658159721f7d084c22cc993de275e664e"</automated>
  </verify>
  <done>G0 through G5 all pass on the current HEAD, and each result's real output tail is quoted in the SUMMARY: the test counts, ruff "All checks passed!", preflight rc=20 with the message, non-preflight rc=1, the workflow mapping lines, and the invariant results. Any fix is folded into its target commit (or a documented fix commit) and all gates were re-run after it.</done>
</task>

<task type="auto">
  <name>Task 3: One docs commit (STATE.md Rebase-Protokoll 2026-09-22 + verified Known-Issue updates), then final HEAD re-check</name>
  <files>.planning/STATE.md, scripts/PAPER_RUN.md (conditional), .planning/MILESTONE.md (conditional); all inside the worktree</files>
  <action>
Per locked decision 7, edit the COMMITTED STATE.md in WT. Do not copy the main tree's dirty STATE.md or its bogus `milestone_name: - **P1` frontmatter.

(1) Change the "Last activity" line to 2026-09-22: rebased onto upstream v0.5.0 (origin/main 2d17df8), with the ported feature count and the gate result.
(2) Add a new section "### Rebase-Protokoll (2026-09-22)" directly ABOVE the existing "Rebase-Protokoll (2026-08-18)" section, using its bullet shape and German prose:
- Basis: v0.5.0 2d17df8. The old strand is backed up as branch backup/pre-rebase-v0.5.0 @16bbe96, and the backup/pre-rebase-v0.3.1 and v0.2.5-tex refs remain.
- Gedroppt: 402cd8b (empty), plus every commit dropped automatically or by resolution, each with its reason.
- Portiert: budget moved onto the #1249 lifecycle (reset in create_run_state before reflection, streamed `_run_graph` plus CLI abort with best-effort partial save, stream_run and clear_run_checkpoint removed because upstream owns them); structured retry; fail-fast; ticker validation (kept or dropped as a duplicate, with the upstream line); the paper run.
- Konfliktauflösungen: one line each, taken from the SUMMARY notes.
- Versionsstrategie: pyproject stays upstream 0.5.0, "Unreleased" section above 0.5.0, no tag.
- Offen: promotion to main plus force-push of the fork is a separate step that the operator must approve.
(3) Re-check Blockers/Concerns and Known Issues against the HEAD code. Mark an item "erledigt upstream" ONLY with a file:line citation taken from HEAD (grep -n in WT):
- CLI-Memory-Parität: expected resolved. create_run_state in trading_graph.py calls _resolve_pending_entries and past_context; cli/main.py calls graph.create_run_state and graph.record_decision.
- Reflexions-Horizont: expected resolved. `_fetch_returns` requires `len(stock) > holding_days` (#1169, 30d42ab), and holding_period_days is configurable (85d9137).
- python-dotenv undeclared: expected resolved, since pyproject declares python-dotenv>=1.0.0.
- CI-Lockfile-Policy (uv sync without --locked): resolve only if .github/workflows/ci.yml at HEAD no longer uses uv sync and uv.lock is gone upstream.
Leave any item you cannot verify unchanged. Update the "no push access / force-push after rebase" blocker only if a fact changed. Do not add a Quick Tasks table row; the orchestrator does that.
(4) scripts/PAPER_RUN.md: if item (3) verified the Reflexions-Horizont fix, rewrite the first "Known limitations" bullet so it states the actual behaviour. An entry now settles only after the full holding window (holding_period_days trading days, default 5) has traded, so with the daily cadence each decision is scored about 5 trading days later, on the next same-ticker run after that. Cite the config key. If it was not verified, leave the file unchanged. The reason for this edit: a documented fact became false, which is the same test decision 7 applies to MILESTONE.md.
(5) .planning/MILESTONE.md: edit only if a statement became false because of v0.5.0. The default expectation is no change; the 2026-08-21 ai_tradex correction is already in the header.
(6) Make one commit in WT covering only these docs files: `git add` the named files explicitly (never `git add -A`), with the message "docs(planning): rebase protocol for upstream v0.5.0" plus a short body, ending with the trailer line `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`. Pass the message via -F from a temp file or with multiple -m flags.
(7) Final HEAD re-check (decision 6 requires the gates on HEAD): re-run `PYTHONPATH=. PY -m pytest -q` and `RUFF check .` in WT and quote the tails. Confirm the last commit touches only .md files. Do NOT write PLAN or SUMMARY into WT (decision 8). The SUMMARY goes to the main repo at .planning/quick/260922-iz5-rebase-strand-onto-upstream-v0-5-0/260922-iz5-SUMMARY.md, and it must include the worktree path, the branch, HEAD SHA, the commit list BASE..HEAD, the dropped commits, the resolution lines, the gate outputs, and a note that promotion to main and the fork force-push are pending operator approval.
  </action>
  <verify>
    <automated>cd /c/Users/admin/PycharmProjects/TradingEngineX-rebase-v050 && grep -q 'Rebase-Protokoll (2026-09-22)' .planning/STATE.md && ! grep -q 'milestone_name: - \*\*P1' .planning/STATE.md && test -z "$(git diff --name-only HEAD~1 HEAD | grep -v '\.md$')" && git log -1 --format=%B | grep -q 'Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>' && PYTHONPATH=. /c/Users/admin/PycharmProjects/TradingEngineX/.venv/Scripts/python.exe -m pytest -q && /c/Users/admin/PycharmProjects/TradingEngineX/.venv/Scripts/ruff.exe check . && test -z "$(git status --porcelain)" && test ! -e .planning/quick/260922-iz5-rebase-strand-onto-upstream-v0-5-0/260922-iz5-SUMMARY.md && test "$(git -C /c/Users/admin/PycharmProjects/TradingEngineX rev-parse main)" = "16bbe96658159721f7d084c22cc993de275e664e"</automated>
  </verify>
  <done>The branch HEAD is one docs-only commit (with the trailer) on top of the rebased strand. STATE.md has the 2026-09-22 protocol, and each resolved Known Issue carries a file:line citation from HEAD. PAPER_RUN.md and MILESTONE.md were changed only where a fact was verifiably false. pytest and ruff pass again on the final HEAD. The WT working tree is clean. main and fork/main are unmoved (an operator-caused move is reported). The SUMMARY lives in the main repo's quick-task directory, not in WT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local branch -> fork/origin remotes | Publishing rewritten history is irreversible for consumers; it is out of scope for this task |
| main working tree (operator-owned, dirty) -> task | The operator edits and commits there in parallel; any write there could destroy their work |
| upstream v0.5.0 code -> our conflict resolutions | A careless resolution could silently revert an upstream fix (security or checkpoint lifecycle) |
| paper-run script -> LLM providers | A run with real credentials would spend money and hit the network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-iz5-01 | Tampering | git history of main / fork/main / tags | mitigate | All rewrites happen in the separate worktree on rebase/upstream-v0.5.0. The backup branch is set at 16bbe96 before starting. No push, reset, checkout or rebase runs in MAIN, and G5 checks that main, fork/main and tags are unmoved |
| T-iz5-02 | Tampering | operator's uncommitted files in MAIN | mitigate | The only writes to MAIN are `git branch` and `git worktree add`. STATE.md is edited in WT from the committed version. G5 checks that the operator's porcelain entries are still present |
| T-iz5-03 | Elevation / Tampering | conflict resolution reverting upstream fixes (#1249 lifecycle, bbcd666 sidecars, safe_ticker_component path checks) | mitigate | Upstream-wins policy. `git diff --diff-filter=D BASE HEAD` must be empty. The changed-file allowlist is checked in G4. Upstream test files are unmodified except the documented test_memory_log stub. Upstream's test_checkpoint_lifecycle must stay green |
| T-iz5-04 | Elevation | CLI ticker -> filesystem path (path traversal, #618 class) | mitigate | Ticker validation stays: either ours before any disk write, or upstream's equivalent cited by line. Upstream safe_ticker_component in `_log_state` and report paths stays untouched |
| T-iz5-05 | Information disclosure / cost | paper-run gate | mitigate | Preflight only, all provider key vars unset with env -u, no .env in WT, temp out/state dirs outside the repos, no paper-log branch involvement |
| T-iz5-SC | Tampering | package installs | accept | No install happens (upstream v0.5.0 only removed deps), so the existing .venv is used as-is. The G4 grep proves our code does not import any removed dep |
</threat_model>

<verification>
- Worktree `C:/Users/admin/PycharmProjects/TradingEngineX-rebase-v050` on `rebase/upstream-v0.5.0`, which contains 2d17df8. No rebase is in progress and the working tree is clean.
- `backup/pre-rebase-v0.5.0` == 16bbe96. main and fork/main are unmoved; no new tags; nothing pushed.
- G0 to G5 pass on the final HEAD, with real output quoted in the SUMMARY.
- The final commit is docs-only, carries the Co-Authored-By trailer, and STATE.md has the 2026-09-22 protocol.
</verification>

<success_criteria>
- Every surviving strand commit sits on v0.5.0. 402cd8b and upstream-duplicate commits are dropped, each with a recorded reason.
- The budget (#582) works on both propagate and CLI via upstream's #1249 lifecycle; our stream_run and clear_run_checkpoint are gone.
- Structured retry, fail-fast credentials, the paper run, and ticker validation (ours or upstream's) are intact.
- pytest -q green, ruff clean, paper-run preflight rc=20 without credentials.
- Every conflict resolution is traceable in the SUMMARY. Known Issues are updated only with file:line evidence.
</success_criteria>

<output>
Create `C:/Users/admin/PycharmProjects/TradingEngineX/.planning/quick/260922-iz5-rebase-strand-onto-upstream-v0-5-0/260922-iz5-SUMMARY.md` (main repo, NOT the worktree) when done.
</output>
