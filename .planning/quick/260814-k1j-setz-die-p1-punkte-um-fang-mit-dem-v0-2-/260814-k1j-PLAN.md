---
phase: quick-260814-k1j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - README.md
  - pyproject.toml
  - .github/workflows/ci.yml
  - tradingagents/budget.py
  - tradingagents/default_config.py
  - tradingagents/graph/trading_graph.py
  - tradingagents/agents/utils/structured.py
  - tradingagents/llm_clients/factory.py
  - cli/main.py
  - tests/test_budget.py
  - tests/test_memory_log.py
  - tests/test_structured_agents.py
  - tests/test_api_key_failfast.py
autonomous: true
requirements: [P1-1, P1-2, P1-3, P1-4, P1-5]

must_haves:
  truths:
    - "CHANGELOG hat eine [0.2.5]-Sektion (Security #618 VOR Added), pyproject.toml steht auf 0.2.5, Compare-Link + README-News-Zeile vorhanden — ohne git tag, ohne git push (D1)"
    - "CI-Workflow existiert: pytest-Matrix ubuntu 3.10-3.13 + windows 3.13 mit plain `uv sync`, ruff nur E9/F63/F7/F82, pip-audit advisory (D2/D3)"
    - "Ein Run mit gesetztem max_cost_per_run oder max_tokens_per_run bricht VOR dem naechsten LLM-Call mit BudgetExceededError ab; der letzte Graph-State wird in BEIDEN Pfaden via gemeinsamer stream_run-Methode + _log_state partial gespeichert (CLI streamt nicht mehr an propagate() vorbei); --checkpoint ist damit auch im CLI-Pfad real wirksam und die CLI-Abbruchmeldung stimmt (D4)"
    - "max_cost_per_run ohne konfigurierte model_cost_rates failt sofort beim Graph-Bau (build_spend_tracker -> BudgetConfigError) — Python-API UND CLI, nie stilles 0-Kosten-Zaehlen (D4)"
    - "Schema-Fehler im Entscheider-Trio: 1 Retry mit Fehlerkontext, dann Prosa-Fallback — nie Run-Abbruch; BudgetExceededError wird dabei NICHT verschluckt (D5)"
    - "Fehlender/leerer API-Key des gewaehlten Providers bricht beim Start (create_llm_client) mit klarer Meldung ab — CLI und Python-API; expliziter api_key-kwarg ersetzt NUR die Key-Pruefung, Azure-Endpoint/Deployment werden weiterhin validiert (D6)"
    - "uv.lock, .gitignore, .mcp.json, .idea/, .serena/, scripts/graphify.sh unveraendert; genau 5 atomare Commits mit expliziten Pfaden (D7)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Release-Sektion 0.2.5"
      contains: "## [0.2.5] — 2026-08-14"
    - path: "pyproject.toml"
      provides: "Version 0.2.5 + PEP-735 dev-Gruppe"
      contains: "[dependency-groups]"
    - path: ".github/workflows/ci.yml"
      provides: "CI: test-Matrix, lint, audit"
      contains: "astral-sh/setup-uv@v10"
    - path: "tradingagents/budget.py"
      provides: "SpendTracker-Callback (raise_error=True, defensive Extraktion), BudgetExceededError, BudgetConfigError, Rate-Aufloesung + Rate-Validierung"
      contains: "raise_error = True"
      min_lines: 60
    - path: "tradingagents/graph/trading_graph.py"
      provides: "Gemeinsame Stream-Methode fuer CLI + Python-API (Checkpointer, Last-State, Partial-Save)"
      contains: "def stream_run"
    - path: "tradingagents/agents/utils/structured.py"
      provides: "Retry mit Fehlerkontext vor Prosa-Fallback"
      contains: "BudgetExceededError"
    - path: "tradingagents/llm_clients/factory.py"
      provides: "Fail-fast Key-Validierung in create_llm_client"
      contains: "MissingAPIKeyError"
    - path: "tests/test_budget.py"
      provides: "Tests: Zaehlung, Abort, Partial-Save (unbound stream_run), Rate-Fail-fast, Defensiv-Extraktion"
      min_lines: 40
    - path: "tests/test_api_key_failfast.py"
      provides: "Tests: fehlender/leerer Key -> Fehler beim Start; Azure-Teilvalidierung trotz api_key-kwarg"
      min_lines: 25
  key_links:
    - from: "tradingagents/graph/trading_graph.py"
      to: "tradingagents/budget.py"
      via: "build_spend_tracker in __init__ (inkl. Rate-Validierung) + BudgetExceededError-Partial-Save in stream_run"
      pattern: "from tradingagents\\.budget import"
    - from: "cli/main.py"
      to: "tradingagents/graph/trading_graph.py stream_run"
      via: "CLI-Stream-Loop laeuft ueber graph.stream_run statt graph.graph.stream (Checkpointer + Partial-Save im CLI-Pfad)"
      pattern: "stream_run"
    - from: "cli/main.py"
      to: "tradingagents/budget.py"
      via: "except BudgetConfigError um Graph-Bau + except BudgetExceededError um Stream-Loop"
      pattern: "BudgetExceededError"
    - from: "tradingagents/agents/utils/structured.py"
      to: "tradingagents/budget.py"
      via: "explizites Re-raise vor generischem except"
      pattern: "except BudgetExceededError"
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml [dependency-groups]"
      via: "plain uv sync (NIE --locked/--frozen — uv.lock passt nach dependency-groups nicht mehr)"
      pattern: "uv sync"
    - from: "tradingagents/llm_clients/factory.py"
      to: "tradingagents/llm_clients/openai_client.py _PROVIDER_CONFIG"
      via: "Env-Var-Map wiederverwenden, nicht duplizieren"
      pattern: "_PROVIDER_CONFIG"
---

<objective>
Alle fuenf P1-Punkte aus .planning/MILESTONE.md umsetzen, in D7-Reihenfolge: (1) Release v0.2.5, (2) CI + Dev-Deps, (3) Kosten-Budget-Enforcement (#582), (4) Schema-Haertung (#583), (5) Secrets-Doku + Fail-fast-Key-Validierung.

Purpose: Framework releasefaehig und betriebssicher machen — unveröffentlichter Security-Fix (#618) muss raus, unbeaufsichtigter Paper-Trading-Betrieb braucht hartes Budget + robuste Schema-Behandlung + Fail-fast-Konfiguration.
Output: 5 atomare Commits auf dem Arbeits-Branch; Tag v0.2.5 setzt der Orchestrator NACH dem Merge auf den Release-Commit (Task 1) — nicht der Executor.
</objective>

<context>
@.planning/MILESTONE.md
@.planning/quick/260814-k1j-setz-die-p1-punkte-um-fang-mit-dem-v0-2-/260814-k1j-CONTEXT.md
@.planning/quick/260814-k1j-setz-die-p1-punkte-um-fang-mit-dem-v0-2-/260814-k1j-RESEARCH.md

<interfaces>
<!-- Verifizierte Vertraege aus der Codebasis — direkt nutzen, keine Exploration noetig. -->

Von tradingagents/agents/utils/structured.py (heutiger Stand, Z. 31-73):
```python
def bind_structured(llm, schema: type[T], agent_name: str) -> Optional[Any]: ...
def invoke_structured_or_freetext(structured_llm, plain_llm, prompt, render, agent_name) -> str:
    # heute: 1 Versuch structured -> except Exception -> sofort plain_llm.invoke(prompt).content
    # prompt ist str (RM/PM) ODER Liste von Message-Dicts (Trader)
```

Von cli/stats_handler.py (Token-Extraktions-Muster, Z. 40-56 — fuer SpendTracker uebernehmen, NICHT importieren):
```python
def on_llm_end(self, response: LLMResult, **kwargs):
    generation = response.generations[0][0]           # in try/except (IndexError, TypeError) -> return
    message = generation.message                       # nur via hasattr
    usage_metadata = message.usage_metadata            # AIMessage; None-Check vor .get
    tokens_in  += usage_metadata.get("input_tokens", 0)
    tokens_out += usage_metadata.get("output_tokens", 0)
```

Von tradingagents/graph/trading_graph.py:
- Z. 14: `create_llm_client` wird als MODUL-SYMBOL importiert -> Tests, die TradingAgentsGraph instanziieren, muessen `tradingagents.graph.trading_graph.create_llm_client` patchen (NICHT factory.*)
- Z. 70: `self.callbacks = callbacks or []` -> Z. 83-84: `llm_kwargs["callbacks"] = self.callbacks` -> Konstruktor aller LLM-Clients (alle 4 Familien reichen "callbacks" durch)
- Z. 265-301 `propagate`: setzt self.ticker, Checkpointer-Setup (compile mit SqliteSaver, Resume-Logging) + try/finally-Cleanup um `self._run_graph(...)`
- Z. 303-348 `_run_graph`: baut init_state (mit past_context) + args, injiziert thread_id (wenn checkpoint_enabled); debug -> `self.graph.stream(...)` (Chunks = voller State je Node, `trace[-1]` = final); non-debug -> `self.graph.invoke(...)`; Erfolgs-Postprocessing: curr_state, _log_state, memory_log.store_decision, clear_checkpoint (NUR bei Erfolg -> Abort bleibt resumefaehig)
- Z. 350-390 `_log_state(trade_date, final_state)`: indiziert heute hart ~15 State-Keys, nutzt self.ticker, schreibt JSON mit encoding="utf-8"

WICHTIG (Codex-verifiziert): cli/main.py:1056 streamt heute DIREKT `graph.graph.stream(...)` und umgeht propagate() — Checkpointer wird im CLI-Pfad nie initialisiert, thread_id nie gesetzt, _log_state nie aufgerufen. `--checkpoint` ist im CLI-Direktstream heute ein toter Config-Schalter.

Von tests/test_memory_log.py:767-769 (Muster fuer Graph-Methoden-Tests OHNE Voll-Instanziierung):
```python
mock_graph = MagicMock(spec=TradingAgentsGraph)
result = TradingAgentsGraph._resolve_pending_entries(mock_graph, "AAPL")  # unbound call
```

Von tradingagents/llm_clients/factory.py: `create_llm_client(provider, model, base_url=None, **kwargs)` — lazy imports je Provider-Zweig; `_OPENAI_COMPATIBLE = ("openai","xai","deepseek","qwen","glm","ollama","openrouter")`

Von tradingagents/llm_clients/openai_client.py (Z. 113-120):
```python
_PROVIDER_CONFIG = {
    "xai": ("https://api.x.ai/v1", "XAI_API_KEY"),
    "deepseek": ("https://api.deepseek.com", "DEEPSEEK_API_KEY"),
    "qwen": ("...", "DASHSCOPE_API_KEY"),
    "glm": ("...", "ZHIPU_API_KEY"),
    "openrouter": ("...", "OPENROUTER_API_KEY"),
    "ollama": ("http://localhost:11434/v1", None),   # kein Key
}
```

Von cli/main.py:
- Z. 929 `def run_analysis(checkpoint: bool = False)`; Z. 934-946 config-Aufbau aus selections; Z. 956-961 `TradingAgentsGraph(..., callbacks=[stats_handler])` (VOR dem Live-Layout); Z. 999-1015 save_report_section_decorator (Sektionen landen inkrementell als reports/*.md); Z. 1047-1056 init_state + `graph.propagator.get_graph_args(callbacks=[stats_handler])` + Direktstream; Z. 1200-1217 `@app.command() analyze` mit `--checkpoint`-Typer-Option (Vorbild fuer `--max-cost`)

Von tests/conftest.py: autouse `_dummy_api_keys` setzt Platzhalter fuer 10 Env-Vars (`_API_KEY_ENV_VARS`) — Fail-fast-Tests brauchen `monkeypatch.delenv(..., raising=False)`. Fixture `mock_llm_client` patcht `tradingagents.llm_clients.factory.create_llm_client` (fuer Graph-Instanziierung siehe Modul-Symbol-Hinweis oben).

LangChain-Mechanik (verifiziert, .venv langchain_core 1.3.2, callbacks/manager.py:306-314): `BaseCallbackHandler.raise_error` default False -> Handler-Exceptions werden GESCHLUCKT (nur logger.warning). Budget-Handler MUSS `raise_error = True` als Klassenattribut setzen. Kehrseite: mit raise_error=True bricht JEDE unbehandelte Exception im Handler (z. B. `None.get()`) den Run ab — alle Extraktionen defensiv.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Release v0.2.5 — CHANGELOG, Version-Bump, README-News (P1-1, D1)</name>
  <files>CHANGELOG.md, pyproject.toml, README.md</files>
  <action>
    Per D1 — nur Doku/Metadaten, kein Code, KEIN git tag, KEIN git push (Tag setzt der Orchestrator nach Merge).

    1. pyproject.toml Zeile 7: `version = "0.2.4"` -> `version = "0.2.5"`. Sonst nichts in dieser Datei aendern (dependency-groups kommt erst in Task 2, getrennter Commit).
    2. CHANGELOG.md: Neue Sektion direkt VOR `## [0.2.4] — 2026-04-25` (Zeile 9) einfuegen. Header exakt `## [0.2.5] — 2026-08-14` (Gedankenstrich U+2014, nicht Bindestrich — Formatkonvention, Codex-Review achtet darauf). Sektionsreihenfolge: `### Security` ZUERST (Praezedenz 0.2.1), dann `### Added`. Eintraege im Hausstil (Fliesstext mit Fett-Lead, Issue-Nr. in Klammern):
       - Security: Ticker-Validierung vor Pfadverwendung — Ticker, die als Pfadkomponente aus dem results-Verzeichnis ausbrechen wuerden, werden abgelehnt (Path-Traversal-Schutz). (#618) [Commit 2c97bad]
       - Added: DeepSeek V4 Thinking-Mode-Round-Trip via `DeepSeekChatOpenAI`-Subklasse. [Commit 7e9e7b8]
    3. CHANGELOG.md Compare-Links (Block ab Zeile 260): neue erste Zeile `[0.2.5]: https://github.com/TauricResearch/TradingAgents/compare/v0.2.4...v0.2.5` vor der `[0.2.4]:`-Zeile.
    4. README.md `## News` (Zeile 31): neue erste Bullet-Zeile im bestehenden Format: `- [2026-08] **TradingAgents v0.2.5** released with a ticker path-traversal security fix and DeepSeek V4 thinking-mode support. See [CHANGELOG.md](CHANGELOG.md) for the full list.` (RESEARCH Open Question 3, RESOLVED — Konvention seit 0.2.0.)
    5. Commit NUR mit expliziten Pfaden (nie -A/-u; uv.lock und .gitignore sind working-tree-modified und duerfen nicht mitrutschen): `git add CHANGELOG.md pyproject.toml README.md` dann `git commit -m "chore: release v0.2.5 — ticker path security fix (#618), DeepSeek V4 thinking mode"`.
    6. Known Issue fuer SUMMARY notieren (NICHT fixen): `python-dotenv` ist undeklariert (cli/main.py:7 importiert dotenv, kommt nur transitiv) — Fix wuerde uv.lock aendern, Kandidat v0.2.6/P2.
  </action>
  <verify>
    <automated>test "$(grep -c '^version = "0.2.5"' pyproject.toml)" -eq 1 && test "$(grep -c '\[0.2.5\]' CHANGELOG.md)" -eq 2 && test "$(git show --stat --format='' HEAD | grep -cE 'CHANGELOG\.md|pyproject\.toml|README\.md')" -eq 3 && test -z "$(git tag -l v0.2.5)" && PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>
  <done>Verify-Kette exit 0 (pytest: 108 passed, 0 failed). Release-Commit existiert mit exakt 3 Dateien; CHANGELOG-Sektion 0.2.5 mit Security vor Added + Compare-Link (grep-Count 2 = Header + Link); README-News-Zeile; kein Tag, kein Push.</done>
</task>

<task type="auto">
  <name>Task 2: CI-Paket — GitHub-Actions-Workflow + PEP-735 Dev-Deps (P1-2, D2/D3)</name>
  <files>.github/workflows/ci.yml, pyproject.toml</files>
  <action>
    Per D2/D3. WICHTIG: Lokal KEIN `uv sync`/`uv lock` ausfuehren (uv.lock darf sich nicht aendern; Maschine hat SSL-Probleme mit uv — Memory). Verifikation lokal via tomllib/grep + Testsuite (KEIN `import yaml` — PyYAML ist nicht direkt deklariert).

    1. pyproject.toml: ans Dateiende neue Sektion `[dependency-groups]` mit `dev = ["pytest", "ruff"]` (PEP 735, uv-nativ). Keine weiteren Tools (pip-audit laeuft via uvx in CI).
    2. Neue Datei `.github/workflows/ci.yml` (Verzeichnis existiert nicht — anlegen). Aufbau:
       - `name: CI`; Trigger: `push` auf `main` + `pull_request`; `permissions: contents: read`; `concurrency`-Gruppe `ci-${{ github.ref }}` mit `cancel-in-progress: true`.
       - Job `test`: `strategy.fail-fast: false`; Matrix `os: [ubuntu-latest]` x `python-version: ["3.10", "3.11", "3.12", "3.13"]` PLUS `include`-Eintrag `os: windows-latest, python-version: "3.13"` (Windows-Encoding-Historie des Projekts). `runs-on: ${{ matrix.os }}`. Steps: `actions/checkout@v5`; `astral-sh/setup-uv@v10` mit Input `python-version: ${{ matrix.python-version }}`; dann `uv sync` (PLAIN — niemals `--locked`/`--frozen`: das committete uv.lock kennt die neue dependency-group nicht und darf nicht regeneriert werden; plain sync re-resolved runner-lokal, Pitfall 3 aus RESEARCH); dann `uv run pytest tests/ -q` (Projekt wird von uv sync installiert, kein PYTHONPATH-Trick noetig; Keys nicht noetig — conftest setzt Platzhalter). Falls die dev-Gruppe wider Erwarten nicht default-installiert wird (RESEARCH Annahme A2): Fallback `uv sync --group dev` in den Step schreiben — Kommentar im YAML dazu.
       - Job `lint`: ubuntu-latest, Python 3.13, checkout + setup-uv + `uv sync`, dann `uv run ruff check --select E9,F63,F7,F82 .` — NUR diese kritischen Regeln (Tag-1-gruen, Ausbau spaeter).
       - Job `audit`: ubuntu-latest, `continue-on-error: true` (advisory per D2, Eskalation auf blocking ist P2/Runbook). Steps: checkout + setup-uv, dann `uv export --format requirements-txt --no-emit-project -o audit-requirements.txt` (re-resolved runner-lokal, committetes uv.lock bleibt unberuehrt), dann `uvx pip-audit -r audit-requirements.txt`.
    3. Commit: `git add .github/workflows/ci.yml pyproject.toml` dann `git commit -m "ci: add GitHub Actions (pytest matrix, ruff critical rules, pip-audit advisory) + dev dependency group"`. Hinweis fuer SUMMARY: CI-Commit liegt bewusst NACH dem Release-Commit — der Orchestrator taggt den Task-1-Commit, CI gehoert zu v0.2.6 (D2).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['dependency-groups']['dev']==['pytest','ruff'] and d['project']['version']=='0.2.5'" && test "$(grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'astral-sh/setup-uv@v10')" -ge 3 && test "$(grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'windows-latest')" -ge 1 && test "$(grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c '"3.10"')" -ge 1 && test "$(grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'continue-on-error: true')" -eq 1 && test "$(grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -cE 'uv sync --(locked|frozen)')" -eq 0 && test "$(grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'E9,F63,F7,F82')" -ge 1 && PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>
  <done>Verify-Kette exit 0 (pytest: 108 passed). ci.yml enthaelt test/lint/audit-Jobs mit Matrix ubuntu 3.10-3.13 + windows 3.13, plain uv sync (kein --locked/--frozen), ruff nur E9/F63/F7/F82, pip-audit advisory; pyproject hat dev-Gruppe; uv.lock unveraendert (`git status --porcelain uv.lock` zeigt weiterhin nur die vorbestehende Operator-Modifikation, nichts staged); atomarer Commit mit exakt 2 Dateien.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Kosten-Budget pro Run erzwingen — SpendTracker, gemeinsame stream_run-Methode, Abort + Partial-Save, --max-cost (P1-3, D4)</name>
  <files>tradingagents/budget.py, tradingagents/default_config.py, tradingagents/graph/trading_graph.py, cli/main.py, tests/test_budget.py, tests/test_memory_log.py</files>
  <behavior>
    - SpendTracker.raise_error ist True (Klassenattribut) — Regression-Guard gegen LangChains Exception-Schlucken
    - Tokens akkumulieren via on_llm_end/usage_metadata; Ueberschreitung von max_tokens -> naechster on_chat_model_start/on_llm_start raist BudgetExceededError (check-before-spend)
    - Kostenpfad: model_cost_rates angewandt (USD pro 1M Input-/Output-Tokens je Modell); Ueberschreitung von max_cost -> BudgetExceededError
    - Defensiv-Test: LLMResult mit llm_output=None, ohne generations bzw. ohne usage_metadata -> on_llm_end crasht NICHT (mit raise_error=True wuerde jeder unguarded Zugriff den Run abbrechen)
    - build_spend_tracker(config): beide Limits None -> None; max_tokens allein -> SpendTracker ohne Rates-Pflicht; max_cost_per_run gesetzt + Rates fuer deep/quick-Modell fehlen -> BudgetConfigError SOFORT (Fail-fast beim Graph-Bau, Python-API abgedeckt — nie stilles 0-Kosten-Zaehlen)
    - stream_run-Abort (unbound-Muster nach tests/test_memory_log.py:767-769): MagicMock(spec=TradingAgentsGraph) mit echtem config-dict, propagator- und graph-Mocks; graph.stream = Generator, der 1 values-Chunk yieldet und dann BudgetExceededError raist; Konsum von TradingAgentsGraph.stream_run(mock_graph, "AAPL", "2026-01-01", ...) -> pytest.raises(BudgetExceededError) UND mock_graph._log_state wurde genau einmal mit dem letzten Chunk aufgerufen
  </behavior>
  <action>
    Per D4 + gelockte Entscheidungen: Budget-Logik in NEUEM Modul `tradingagents/budget.py` (cli/ importiert von dort, nie umgekehrt — cli/stats_handler.py bleibt unveraendert Anzeige-only). Kern-Refactor (Codex HIGH 1+2): CLI und Python-API laufen kuenftig ueber EINE gemeinsame Stream-Methode `stream_run` in trading_graph.py — der heutige CLI-Direktstream (cli/main.py:1056) umgeht propagate() und damit Checkpointer, thread_id und _log_state komplett; `--checkpoint` ist dort heute wirkungslos. Nach dem Umbau stimmen CLI- und Python-API-Verhalten ueberein (Checkpointer + Partial-Save in beiden Pfaden).

    1. Neues Modul `tradingagents/budget.py`:
       - `class BudgetExceededError(RuntimeError)`: traegt tokens_in, tokens_out, cost, limit-Beschreibung in der Message.
       - `class BudgetConfigError(ValueError)`: fuer fehlende Rates bei aktivem max_cost.
       - `class SpendTracker(BaseCallbackHandler)`: Klassenattribut `raise_error = True` (KRITISCH — sonst schluckt langchain_core die Exception, Pitfall 1). Konstruktor `(max_cost=None, max_tokens=None, model_cost_rates=None)`. Thread-safe mit threading.Lock (Muster StatsCallbackHandler). ALLE Extraktionen defensiv (Codex MEDIUM 5 — mit raise_error=True bricht jede Handler-Exception den Run ab): `on_llm_end` exakt nach dem Muster cli/stats_handler.py:40-56 (generations-Zugriff in try/except (IndexError, TypeError) -> return; message nur via hasattr; usage_metadata-None-Check vor .get). Modellname fuer Kosten: `(response.llm_output or {}).get("model_name")` mit Fallback `(getattr(message, "response_metadata", None) or {}).get("model_name")` — nie unguarded `.get()` auf potenziellem None. Rate-Aufloesung `_resolve_rate(model_name)` gegen `model_cost_rates` (Format: `{modellname: {"input": usd_pro_1M, "output": usd_pro_1M}}`); unbekanntes/fehlendes Modell -> einmalige Warnung, 0-Kosten-Beitrag (nach der Konstruktions-Validierung unten ein Randfall, kein Normalpfad). Budget-Pruefung in `on_chat_model_start` UND `on_llm_start`: akkumulierte Kosten > max_cost ODER (tokens_in+tokens_out) > max_tokens -> `raise BudgetExceededError(...)` BEVOR der naechste teure Call rausgeht.
       - `def ensure_rates_configured(model_cost_rates, model_names)`: fuer jeden Namen pruefen, dass input+output-Rates existieren; sonst BudgetConfigError mit Meldung, welches Modell fehlt und wie `model_cost_rates` zu konfigurieren ist.
       - `def build_spend_tracker(config) -> Optional[SpendTracker]`: liest `max_cost_per_run`, `max_tokens_per_run`, `model_cost_rates`; beide Limits None -> None. WICHTIG (Codex HIGH 3): wenn `max_cost_per_run` gesetzt -> selbst `ensure_rates_configured(model_cost_rates, [config["deep_think_llm"], config["quick_think_llm"]])` aufrufen — die Validierungsautoritaet liegt HIER, damit auch Python-API-Nutzer (die config direkt setzen) sofort beim Graph-Bau einen klaren Fehler bekommen statt stillem 0-Kosten-Zaehlen. `max_tokens_per_run` allein braucht keine Rates.
    2. `tradingagents/default_config.py`: neue Keys mit Kommentaren: `"max_cost_per_run": None` (USD, None = aus; braucht model_cost_rates), `"max_tokens_per_run": None` (Input+Output gesamt, ratenfrei), `"model_cost_rates": {}` (user-supplied, USD pro 1M Tokens — Maintainer-Entscheid #582: keine hartkodierte Preistabelle).
    3. `tradingagents/graph/trading_graph.py`:
       - Import oben: `from tradingagents.budget import BudgetExceededError, build_spend_tracker`.
       - `__init__` nach Zeile 70 (`self.callbacks = callbacks or []`): `self.spend_tracker = build_spend_tracker(self.config)`; wenn nicht None -> an `self.callbacks` anhaengen (landet via llm_kwargs["callbacks"] im LLM-Konstruktor). BudgetConfigError aus build_spend_tracker propagiert bewusst aus __init__ (Fail-fast beim Graph-Bau).
       - NEUE oeffentliche Generator-Methode `stream_run(self, company_name, trade_date, *, init_agent_state=None, graph_args=None)` — kapselt den kompletten Stream-Pfad fuer BEIDE Nutzer (Codex HIGH 1+2):
         a. `self.ticker = company_name` setzen (Pflicht: `_log_state` nutzt self.ticker; der CLI-Pfad setzte ihn bisher nie).
         b. Checkpointer-Setup aus `propagate()` (Z. 278-301) HIERHER verschieben: bei `checkpoint_enabled` -> `get_checkpointer(...)`-Kontext oeffnen, `self.graph = self.workflow.compile(checkpointer=saver)`, Resume-Logging via `checkpoint_step(...)`; gesamter Stream in try/finally: Kontext schliessen + `self.graph = self.workflow.compile()` (Cleanup-Logik 1:1 uebernehmen).
         c. `init_agent_state` default: `self.propagator.create_initial_state(company_name, trade_date, past_context=self.memory_log.get_past_context(company_name))` (wie _run_graph heute); Aufrufer duerfen ein eigenes uebergeben (CLI baut seines ohne past_context — Verhalten unveraendert). `graph_args` default: `self.propagator.get_graph_args()`; thread_id-Injektion bei checkpoint_enabled (Logik aus _run_graph Z. 313-315).
         d. Stream-Loop: `last_state = None; for chunk in self.graph.stream(init_agent_state, **graph_args): last_state = chunk; yield chunk` (gleiche values-Semantik wie der heutige debug-Pfad — jeder Chunk ist der volle State nach einem Node).
         e. `except BudgetExceededError`: wenn last_state vorhanden -> `self.curr_state = last_state` + `self._log_state(trade_date, last_state)` (tolerante Variante, s. u.); `logger.warning("Run aborted by budget limit: %s — partial state saved; resume with --checkpoint", exc)`; re-raise. clear_checkpoint wird NICHT aufgerufen (bleibt Erfolgs-only -> Abort resumefaehig, D4).
       - `_run_graph` refaktorieren: konsumiert `self.stream_run(company_name, trade_date)`; debug-Modus: je Chunk `chunk["messages"][-1].pretty_print()` wie bisher; `final_state` = letzter Chunk; Erfolgs-Postprocessing unveraendert (curr_state, _log_state, memory_log.store_decision, clear_checkpoint, return final_state + process_signal). Der invoke-Pfad entfaellt (gelockte Entscheidung: stream statt invoke).
       - `propagate()` wird schlanker: behaelt `_resolve_pending_entries` + Aufruf `self._run_graph(...)`; Checkpointer-Setup/-Cleanup ist nach stream_run gewandert (kein Doppel-Setup).
       - `_log_state` tolerant machen: alle direkten Key-Zugriffe auf `final_state.get(key, "")` bzw. fuer die verschachtelten debate-states `final_state.get("investment_debate_state", {}).get(..., "")` umstellen — Partial-States mid-run haben die meisten Keys noch nicht. Bestehendes Verhalten fuer vollstaendige States identisch; JSON-Write behaelt encoding="utf-8".
    4. `cli/main.py`:
       - `analyze`-Command (Z. 1200-1217): neue Typer-Option `max_cost: Optional[float] = typer.Option(None, "--max-cost", help="Abort the run once accumulated LLM cost (USD) exceeds this limit; requires model_cost_rates in config.")` — exakt nach dem `--checkpoint`-Vorbild; an `run_analysis(checkpoint=checkpoint, max_cost=max_cost)` durchreichen.
       - `run_analysis(checkpoint=False, max_cost=None)`: nach dem config-Aufbau (Z. 946): wenn `max_cost is not None` -> `config["max_cost_per_run"] = max_cost`. KEIN separater Vorab-Rates-Check in der CLI (Validierungsautoritaet liegt in build_spend_tracker, keine Duplikation): stattdessen die `TradingAgentsGraph(...)`-Konstruktion (Z. 956-961, liegt VOR dem Live-Layout) in try/except BudgetConfigError -> `console.print(f"[red]{exc}[/red]")` + `raise typer.Exit(1)` (klare Meldung, keine stillen Schaetzpreise, D4).
       - Stream-Umstellung (Codex HIGH 1+2): Z. 1047-1056 — init_agent_state weiter selbst bauen, `args = graph.propagator.get_graph_args(callbacks=[stats_handler])` weiter selbst holen; dann `for chunk in graph.stream_run(selections["ticker"], selections["analysis_date"], init_agent_state=init_agent_state, graph_args=args):` statt `graph.graph.stream(...)`. Damit greifen Checkpointer + thread_id + Budget-Partial-Save (_log_state) erstmals auch im CLI-Pfad — `--checkpoint` war im Direktstream bisher wirkungslos (als Bugfix im SUMMARY dokumentieren). Chunk-Verarbeitung im Loop-Body unveraendert.
       - try/except BudgetExceededError um den Stream-Loop: saubere rich-Meldung statt Traceback — Budget erreicht (Grund aus der Exception), letzter Graph-State via _log_state gespeichert, Report-Sektionen liegen inkrementell unter reports/ (save_report_section_decorator), mit `--checkpoint` resumefaehig (Aussage ist nach der stream_run-Umstellung korrekt); danach `return` aus run_analysis (kein Report-Finalisierungscode auf halbem State). Import: `from tradingagents.budget import BudgetConfigError, BudgetExceededError`.
    5. Neue Tests `tests/test_budget.py` gemaess <behavior>. SpendTracker-Tests mit echten langchain_core-Objekten (AIMessage mit usage_metadata, ChatGeneration, LLMResult) oder MagicMock; stream_run-Abort-Test im unbound-Muster von tests/test_memory_log.py:767-769 (`MagicMock(spec=TradingAgentsGraph)` + echtes config-dict + gemocktes `mock_graph.graph.stream` — KEINE Voll-Instanziierung noetig). Falls doch instanziiert wird: patch-Target ist `tradingagents.graph.trading_graph.create_llm_client` (Modul-Symbol-Import Z. 14, Codex MEDIUM 6 — NICHT factory.create_llm_client). Jede Datei-I/O in Tests mit encoding="utf-8".
    6. Bestehenden Regressionstest an das Refactor anpassen: `tests/test_memory_log.py::test_full_pipeline_no_regression` (Z. 730-772) mockt heute `mock_graph.graph.invoke.return_value = fake_state` (Z. 760) und bindet das reale `_run_graph` via functools.partial (Z. 766-768) — nach dem Refactor konsumiert `_run_graph` `self.stream_run(...)`, ein plain MagicMock liefert 0 Chunks -> final_state None -> Crash. Fix: das reale `_run_graph`-Binding beibehalten, aber statt `mock_graph.graph.invoke` jetzt `mock_graph.stream_run` als Generator stubben, z. B. `mock_graph.stream_run = lambda *a, **k: iter([fake_state])` (bzw. functools.partial-Aequivalent — Muster konsistent zum bestehenden Test halten). Sonst nichts an der Datei aendern.
    7. Commit: `git add tradingagents/budget.py tradingagents/default_config.py tradingagents/graph/trading_graph.py cli/main.py tests/test_budget.py tests/test_memory_log.py` dann `git commit -m "feat(budget): enforce per-run cost/token budget with abort and partial save (#582)"`.
  </action>
  <verify>
    <automated>PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_budget.py -q && PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/ -q && .venv/Scripts/python.exe -c "from tradingagents.budget import SpendTracker, BudgetExceededError, BudgetConfigError, build_spend_tracker, ensure_rates_configured; assert SpendTracker.raise_error is True" && test "$(grep -c 'def stream_run' tradingagents/graph/trading_graph.py)" -eq 1 && test "$(grep -c 'stream_run' cli/main.py)" -ge 1 && test "$(grep -c 'graph\.graph\.stream(' cli/main.py)" -eq 0</automated>
  </verify>
  <done>Verify-Kette exit 0 (Testsuite: >=108 passed, 0 failed). Budget-Ueberschreitung bricht den Run vor dem naechsten LLM-Call ab; CLI und Python-API laufen ueber dieselbe stream_run-Methode (Checkpointer + Partial-Save via _log_state in beiden Pfaden, kein Direktstream mehr in cli/main.py); max_cost ohne Rates failt beim Graph-Bau (BudgetConfigError) — CLI zeigt rote Meldung + Exit 1; cli/stats_handler.py unveraendert; atomarer Commit mit exakt 6 Dateien (inkl. Regressionstest-Anpassung in tests/test_memory_log.py).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Schema-Haertung — 1 Retry mit Fehlerkontext, dann Prosa-Fallback (P1-4, D5)</name>
  <files>tradingagents/agents/utils/structured.py, tests/test_structured_agents.py</files>
  <behavior>
    - structured.invoke side_effect=[Exception("bad json"), gueltiges_objekt] -> Ergebnis ist render(gueltiges_objekt); plain_llm wurde NICHT aufgerufen; structured 2x aufgerufen; der 2. Aufruf enthaelt den Fehlertext im Prompt
    - side_effect=[Exception, Exception] -> Prosa-Fallback: plain_llm.invoke mit dem ORIGINAL-Prompt (ohne Fehlerkontext), Rueckgabe response.content, kein Raise
    - Message-Listen-Prompt (Trader-Form): Retry haengt eine zusaetzliche User-Message mit Fehlerkontext an, Original-Liste bleibt unmutiert
    - side_effect=BudgetExceededError -> Exception propagiert (pytest.raises), plain_llm wurde NICHT aufgerufen, KEIN Retry
  </behavior>
  <action>
    Per D5: Scope bleibt das Entscheider-Trio — Umbau ist auf `invoke_structured_or_freetext` in structured.py begrenzt (zentrale Stelle, alle 3 Callsites RM/Trader/PM haengen dran; NICHT alle 13 Nodes anfassen, die Agent-Factories bleiben unveraendert).

    1. `invoke_structured_or_freetext` erweitern:
       - Import `from tradingagents.budget import BudgetExceededError` (Task 3 hat das Modul erstellt — gleiche Package-Seite, Import-Richtung ok).
       - Erster structured-Versuch wie bisher. Im except-Block ZUERST `except BudgetExceededError: raise` (KRITISCH: sonst wuerde der Budget-Abort hier als Schema-Fehler verschluckt und in Retry+Prosa umgemuenzt — Budget-Semantik aus Task 3 muss durchschlagen), DANACH `except Exception as exc:` -> Warnung loggen ("structured-output invocation failed (%s); retrying once with error context") und Retry bauen.
       - Retry: Prompt mit Fehlerkontext anreichern, prompt-form-agnostisch: bei `str` -> Original + zwei Newlines + "Your previous response failed schema validation with this error: {exc}. Respond again and strictly match the required schema."; bei `list` (Message-Dicts, Trader) -> NEUE Liste = Original + angehaengte `{"role": "user", "content": <gleicher Fehlerkontext-Text>}` (Original-Liste nicht mutieren). Zweiter `structured_llm.invoke(retry_prompt)` -> bei Erfolg `render(result)`.
       - Schlaegt auch der Retry fehl (`except BudgetExceededError: raise`, dann `except Exception`): Warnung loggen ("structured retry failed (%s); falling back to free text") und wie bisher `plain_llm.invoke(prompt).content` mit dem ORIGINAL-Prompt (Prosa-LLM ist nicht schema-gebunden, Fehlerkontext waere Rauschen). Schema-Fehler brechen den Run damit nie ab; `bind_structured` bleibt unveraendert.
       - Docstring/Modul-Kommentar (Z. 10-13) an das neue Verhalten anpassen (Schritt 2 der Doku: "retry once with error context, then fall back").
    2. Tests in `tests/test_structured_agents.py` ergaenzen — bestehendes Mock-Muster der Datei (Z. 102-160: MagicMock-LLM, `with_structured_output.return_value = structured`) wiederverwenden; die 4 Faelle aus <behavior> abdecken. Fuer den Fehlerkontext-Assert: `structured.invoke.call_args_list[1]` inspizieren (str-Fall: "bad json" in Prompt enthalten; Listen-Fall: letzte Message ist role=user mit Fehlertext).
    3. Commit: `git add tradingagents/agents/utils/structured.py tests/test_structured_agents.py` dann `git commit -m "feat(structured): retry once with error context before prose fallback (#583)"`.
  </action>
  <verify>
    <automated>test "$(grep -c 'except BudgetExceededError' tradingagents/agents/utils/structured.py)" -ge 2 && PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_structured_agents.py -q && PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>
  <done>Verify-Kette exit 0 (Testsuite: >=108 passed, 0 failed, inkl. 4 neuer Faelle). Retry-dann-Fallback-Kette in structured.py aktiv fuer alle 3 Trio-Callsites; BudgetExceededError wird an beiden Stellen explizit re-raised; bestehende Fallback-Tests weiterhin gruen; atomarer Commit mit exakt 2 Dateien.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Secrets-Doku + Fail-fast-API-Key-Validierung (P1-5, D6)</name>
  <files>tradingagents/llm_clients/factory.py, cli/main.py, README.md, tests/test_api_key_failfast.py</files>
  <behavior>
    - monkeypatch.delenv("OPENAI_API_KEY", raising=False) + create_llm_client("openai", "gpt-5.4") -> MissingAPIKeyError, Message nennt "OPENAI_API_KEY" (Pitfall 5: conftest setzt autouse Platzhalter — delenv im Test noetig)
    - OPENAI_API_KEY="" (leer/whitespace) -> ebenfalls MissingAPIKeyError
    - create_llm_client("ollama", ...) ohne jegliche Keys -> kein Raise bei der Validierung (Client wird konstruiert)
    - create_llm_client("openai", ..., api_key="sk-explicit") bei geloeschter Env-Var -> kein Raise (expliziter kwarg ersetzt die Key-Pruefung)
    - Azure: AZURE_OPENAI_ENDPOINT geloescht -> MissingAPIKeyError, Message listet die fehlende(n) Variable(n)
    - Azure MIT api_key-kwarg, aber geloeschtem AZURE_OPENAI_ENDPOINT -> TROTZDEM MissingAPIKeyError (api_key ersetzt nur die *_API_KEY-Pruefung, nicht Endpoint/Deployment — Codex MEDIUM 4)
  </behavior>
  <action>
    Per D6: Fail-fast zentral in `create_llm_client()` (factory.py) — EIN Hook deckt beide Eintrittspfade ab (CLI-run_analysis -> TradingAgentsGraph.__init__ -> factory; Python-API identisch). Bestehende Provider->Env-Var-Map wiederverwenden, nicht duplizieren.

    1. `tradingagents/llm_clients/factory.py`:
       - `class MissingAPIKeyError(ValueError)` definieren (im factory-Modul, damit sie ohne Heavy-Import erreichbar ist).
       - `def _required_env_vars(provider_lower: str) -> tuple[str, ...]`: fuer OpenAI-kompatible Fremd-Provider (xai/deepseek/qwen/glm/openrouter) den Env-Var-Namen aus `_PROVIDER_CONFIG` ziehen (lazy `from .openai_client import _PROVIDER_CONFIG` INNERHALB der Funktion und nur im kompatiblen Zweig — openai_client wird in diesem Zweig gleich danach ohnehin importiert, das Lazy-Import-Design der factory bleibt fuer anthropic/google/azure erhalten); ollama -> leer; native Provider ergaenzen, wo bisher KEINE Map existiert (RESEARCH: "verstreut"): openai -> ("OPENAI_API_KEY",), anthropic -> ("ANTHROPIC_API_KEY",), google -> ("GOOGLE_API_KEY",), azure -> ("AZURE_OPENAI_API_KEY", "AZURE_OPENAI_ENDPOINT", "AZURE_OPENAI_DEPLOYMENT_NAME") (Quelle: azure_client.py Docstring; OPENAI_API_VERSION bleibt optional).
       - Am Anfang von `create_llm_client` (nach provider_lower): Required-Liste bestimmen; wenn `kwargs.get("api_key")` gesetzt ist -> NUR die Variablen ueberspringen, deren Name auf `_API_KEY` endet (Codex MEDIUM 4: der explizite Key ersetzt ausschliesslich die Key-Pruefung — Azure-ENDPOINT/-DEPLOYMENT werden weiterhin geprueft, vgl. azure_client.py:26-41); verbleibende Vars pruefen via `os.environ.get(var, "").strip()` (leer/whitespace zaehlt als fehlend) und bei Luecken `MissingAPIKeyError` mit klarer Meldung raisen: Provider, fehlende Variable(n), Hinweis auf README "Required APIs" / `.env` (cp .env.example .env). Damit crasht ein fehlkonfigurierter Run beim Start (Graph-__init__) statt beim ersten LLM-Call mitten im Run.
    2. `cli/main.py`: `TradingAgentsGraph(...)`-Konstruktion in run_analysis (Z. 956-961, VOR dem Live-Layout) — den in Task 3 eingefuehrten try/except-Block um `except MissingAPIKeyError` erweitern (gleiches Handling: `console.print(f"[red]{exc}[/red]")` + `raise typer.Exit(1)`, freundliche rich-Meldung statt Stacktrace). Import `from tradingagents.llm_clients.factory import MissingAPIKeyError`.
    3. README.md: Abschnitt "Required APIs" (Z. 135-158) erweitern um Unterabschnitt "Secrets & configuration for production": (a) wohin welche Keys — `.env` via `cp .env.example .env` (nie committen), reine Env-Vars, `.env.enterprise` fuer Azure/Enterprise, Docker: `docker compose` liest `.env` automatisch; Azure-Variablenliste (AZURE_OPENAI_API_KEY, AZURE_OPENAI_ENDPOINT, AZURE_OPENAI_DEPLOYMENT_NAME, optional OPENAI_API_VERSION); (b) Fail-fast-Verhalten: fehlender/leerer Key des gewaehlten Providers bricht sofort beim Start mit Nennung der Env-Var ab; (c) Kosten-/Quota-Hinweise: Alpha-Vantage-Quota (yfinance-Fallback ist Default), Budget-Kontrolle via `--max-cost` bzw. `max_tokens_per_run` + user-supplied `model_cost_rates` (USD pro 1M Tokens) — Querverweis auf Task-3-Feature. (D6 laesst docs/ oder README offen; README gewaehlt, da "Required APIs" dort schon existiert und kein docs/-Verzeichnis vorhanden ist.)
    4. Neue Tests `tests/test_api_key_failfast.py` gemaess <behavior> — jeweils `monkeypatch.delenv(..., raising=False)` bzw. `monkeypatch.setenv(..., "")`; Message-Asserts via `pytest.raises(MissingAPIKeyError, match=...)`. Hinweis: diese Tests rufen create_llm_client DIREKT auf (kein Graph-Bau) — das Patch-Target-Thema aus Codex MEDIUM 6 betrifft sie nicht.
    5. Commit: `git add tradingagents/llm_clients/factory.py cli/main.py README.md tests/test_api_key_failfast.py` dann `git commit -m "feat(config): fail-fast API key validation at startup + production secrets docs"`.
  </action>
  <verify>
    <automated>PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_api_key_failfast.py -q && PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/ -q && test "$(grep -c 'Secrets & configuration for production' README.md)" -eq 1 && test "$(grep -c 'MissingAPIKeyError' tradingagents/llm_clients/factory.py)" -ge 2</automated>
  </verify>
  <done>Verify-Kette exit 0 (Testsuite: >=108 passed, 0 failed). create_llm_client validiert Keys VOR Client-Konstruktion fuer alle 10 Provider (ollama ausgenommen; expliziter api_key-kwarg ersetzt nur *_API_KEY-Pruefungen, Azure-Endpoint/Deployment bleiben validiert); CLI zeigt rote Meldung + Exit 1 statt Traceback; README dokumentiert Secrets-Handling, Fail-fast und Kosten/Quota; atomarer Commit mit exakt 4 Dateien; insgesamt genau 5 neue Commits seit Plan-Start, `git tag -l v0.2.5` weiterhin leer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Env/.env -> LLM-Clients | echte API-Keys liegen lokal in `.env` (gitignored); Validierung liest nur Existenz/Leere, loggt nie Werte |
| Provider-Antwort -> Retry-Prompt | Fehlertext der Schema-Validierung wird in den Retry-Prompt eingebettet |
| CI-Runner -> Repo | GitHub Actions fuehrt uv/uvx-Installationen aus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-k1j-01 | Information Disclosure | Fail-fast-Meldungen (factory.py, cli/main.py) | mitigate | Fehlermeldungen nennen nur Env-Var-NAMEN, nie Werte; `.env` wird nie gelesen/geloggt/committet (explizite git-add-Pfade, D7) |
| T-k1j-02 | Denial of Service (finanziell) | Unbeaufsichtigter Dauerbetrieb ohne Kostenlimit | mitigate | SpendTracker mit `raise_error=True`, check-before-spend in on_llm_start/on_chat_model_start; Rates-Fail-fast beim Graph-Bau verhindert stilles 0-Kosten-Zaehlen (Task 3) |
| T-k1j-03 | Tampering (Prompt-Injection via Fehlertext) | Retry-Prompt in structured.py | accept | Fehlertext stammt aus lokaler Pydantic-Validierung/SDK, nicht aus Nutzereingaben; begrenzte Oberflaeche, Trio-only |
| T-k1j-04 | Elevation of Privilege | CI-Workflow-Permissions | mitigate | `permissions: contents: read`, keine Secrets im Workflow, Actions nur von offiziellen Orgs (actions/, astral-sh/) auf Major-Version gepinnt |
| T-k1j-05 | Denial of Service (Handler-Crash) | SpendTracker mit raise_error=True | mitigate | alle Response-Extraktionen defensiv (`(response.llm_output or {})`, try/except um generations, hasattr/None-Checks) — kein unguarded Zugriff kann den Run abbrechen; eigener Defensiv-Test |
| T-k1j-SC | Tampering (Supply Chain) | pytest/ruff (dev-Gruppe), pip-audit (uvx, CI-only) | mitigate | slopcheck nicht verfuegbar; RESEARCH Package-Legitimacy-Audit: pytest bereits im Projekt in Benutzung; ruff 0.16.3 + pip-audit 2.10.1 gegen PyPI-Registry verifiziert (kanonische Astral-/PyPA-Tools, [ASSUMED]); es findet KEIN lokaler Install statt (nur Deklaration + CI); Codex-Plan-Review (D8) dient als menschliches Gate |
</threat_model>

<verification>
Nach Task 5 (Gesamt-Checks, vom Orchestrator lokal zu wiederholen — Executor-Self-Claims nicht trauen):
1. `PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/ -q` -> >=108 passed (108 Bestand + neue Budget-/Schema-/Fail-fast-Tests), 0 failed.
2. `git log --oneline -5` -> exakt 5 neue Commits in D7-Reihenfolge (release, ci, budget, structured, config), keine Vermischung.
3. `git status --porcelain uv.lock .gitignore .mcp.json scripts/graphify.sh` -> nur die VORBESTEHENDEN Working-Tree-Modifikationen an uv.lock/.gitignore (Operator-Drift), nichts Neues, nichts staged; `git log --oneline -5 -- uv.lock .gitignore .mcp.json .idea .serena scripts/graphify.sh` -> leer.
4. `git tag -l v0.2.5` -> leer (Tag setzt der Orchestrator nach Merge auf den Task-1-Commit).
5. Pfad-Konvergenz: `grep -c 'graph\.graph\.stream(' cli/main.py` -> 0 (CLI laeuft ueber stream_run; Checkpointer + Partial-Save damit in beiden Pfaden; `--checkpoint` im CLI-Pfad erstmals wirksam — als Bugfix im SUMMARY dokumentiert).
6. Known Issue dokumentiert (SUMMARY): python-dotenv undeklariert — bewusst NICHT gefixt (uv.lock-Aenderung), Kandidat v0.2.6/P2.
</verification>

<success_criteria>
- Alle 5 P1-Punkte aus MILESTONE.md umgesetzt, je genau ein atomarer Commit, D7-Reihenfolge eingehalten.
- must_haves-Truths alle beobachtbar erfuellt (siehe Frontmatter) — insbesondere: Partial-Save + Checkpoint-Resumefaehigkeit gelten in CLI- UND Python-API-Pfad (gemeinsame stream_run-Methode), Rates-Fail-fast greift beim Graph-Bau.
- Testsuite gruen mit erweitertem Umfang; kein Tag, kein Push, keine Operator-Dateien angefasst.
- CHANGELOG/README formattreu (Gedankenstrich-Header, Security vor Added, News-Zeilen-Format) — Codex-Diff-Review (D8) findet keine HIGH-Issues zur Formattreue.
</success_criteria>

<output>
Erstelle `.planning/quick/260814-k1j-setz-die-p1-punkte-um-fang-mit-dem-v0-2-/260814-k1j-SUMMARY.md` nach Abschluss (inkl. Known Issue python-dotenv, Bugfix-Notiz "--checkpoint war im CLI-Direktstream wirkungslos, jetzt via stream_run wirksam" und Hinweis an den Orchestrator: annotierten Tag `v0.2.5` mit Message "TradingAgents v0.2.5" + Highlights-Liste auf den Task-1-Commit setzen, Konvention wie v0.2.4).
</output>
